# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Node
node_modules/
dist/
.vite/

# Model + data artifacts
*.npy
backend/data/chunk_embeddings.*

# ...except the M3 projection, which is deliberately COMMITTED: UMAP could produce a different
# (e.g. mirrored) layout on a rebuild, and a token must occupy the same place forever.
!backend/data/vocab_umap_3d.npy

# OS / editor
.DS_Store
.idea/
.vscode/

# Vendored agent skills (frontend-design, apple-design, impeccable).
# Reinstallable from skills-lock.json, which IS committed.
.agents/
.claude/skills/
.codex/
